# Verification Records

Store one durable record per significant verification. Each record must identify the requirement, exact candidate SHA, deployed SHA when applicable, claim, evidence, implementer, verifier, timestamp, result, limitations, and GAJ acceptance state if required.

Records claiming independent verification are invalid when verifier and implementer are the same actual actor/process.
